# Vercel / local proxy — คัดลอกเป็น .env.local แล้วใส่ค่าจริง
# ห้าม commit ไฟล์ .env ที่มี URL จริง

# URL ของ Google Apps Script Web App (Deploy > Web app)
GAS_WEB_APP_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
